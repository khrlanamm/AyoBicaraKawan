<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/historyFragmentLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="32dp"
    android:background="@color/background_abk"
    tools:context=".ui.history.HistoryFragment">

    <!-- Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        android:elevation="4dp"
        app:title="@string/title_history"
        app:titleTextColor="@color/black"
        app:navigationIcon="@drawable/icon_back"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:contentInsetStart="0dp"
        app:contentInsetStartWithNavigation="0dp" />

    <!-- RecyclerView untuk menampilkan daftar riwayat laporan -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_report_history"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        tools:listitem="@layout/item_report_history" />

    <!-- ProgressBar untuk indikator loading -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:indeterminateTint="@color/purple_abk"
        app:layout_constraintTop_toTopOf="@id/rv_report_history"
        app:layout_constraintBottom_toBottomOf="@id/rv_report_history"
        app:layout_constraintStart_toStartOf="@id/rv_report_history"
        app:layout_constraintEnd_toEndOf="@id/rv_report_history"
        tools:visibility="visible"/>

</androidx.constraintlayout.widget.ConstraintLayout>
